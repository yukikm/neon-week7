# Solana Signer Swap Demo | React

For start ui should add `.env` (see `.env.example`).

```shell
yarn install
yarn dev
yarn build
```
For run tests should add `/tests/.env` (see `/tests/.env.example`)

```shell
yarn test
```
